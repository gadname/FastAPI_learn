# This file makes Python treat the directory as a package.
# You can optionally import utility functions here if you want to access them as:
# from app.tests.utils import get_random_user_create_schema

# from .user import get_random_user_create_schema, random_lower_string, random_email
